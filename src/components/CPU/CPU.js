import React from 'react';

class CPU extends React.Component {
  constructor (props) {
    super(props);
    return ;
  }
  render () {
    return (
      <React.Fragment>
        <p>{ "CPU" }</p>
      </React.Fragment>
    );
  }
}

export default CPU;
