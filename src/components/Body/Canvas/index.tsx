import React from 'react';
import { CanvasContainer } from './styles';
import { CANVAS_ID } from '../../../utils/constants';

const Canvas = () => {
    return (
        <CanvasContainer id={CANVAS_ID} />
    );
};

export default Canvas;
